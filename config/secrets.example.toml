# API Keys 配置（复制为 secrets.toml 并填入你的 Key）

[api_keys]
primary = "sk-your-primary-api-key"
secondary = "sk-your-secondary-api-key"
embedding = "sk-your-embedding-api-key"

[browser]
browser_use_api_key = ""

[admin]
# 首次启动时会提示设置密码，也可以在这里预设（bcrypt 哈希）
password_hash = ""
jwt_secret = "change-me-to-a-random-string"

[wechat]
api_key = ""
